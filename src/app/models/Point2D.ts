/**
 * Represents (x,y) coordinates of a point in 2D plain
 */
export class Point2D {
    x: number;
    y: number;

    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }
}
